{
  "name": "realm-of-crests",
  "type": "module",
  "version": "1.2.12",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "validate": "node scripts/validate-phase0.mjs",
    "check": "npm run validate && npm run build"
  },
  "dependencies": {
    "astro": "^5.0.0"
  }
}
