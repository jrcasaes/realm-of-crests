{
  "name": "realm-of-crests",
  "type": "module",
  "version": "1.2.12",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "validate:canon": "node scripts/validate-phase0.mjs",
    "validate:experience": "node scripts/validate-phase1.mjs",
    "validate": "npm run validate:canon && npm run validate:experience",
    "check": "npm run validate && npm run build"
  },
  "dependencies": {
    "astro": "^5.0.0"
  }
}
